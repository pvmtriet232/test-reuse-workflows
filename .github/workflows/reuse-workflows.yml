on:
  [push]
jobs:
  call-workflows-in-another-repo:
    uses: pvmtriet232/github-action/.github/workflows/reusing-workflows.yml@main
    with:
      config-path: .github/labeler.yml
    secrets:
      token: ${{ secrets.GITHUB_TOKEN}}

